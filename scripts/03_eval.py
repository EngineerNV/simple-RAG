"""
03_eval.py

This script is a template for evaluating the faithfulness or correctness of RAG (Retrieval-Augmented Generation) outputs.

Instructions:
- Use this script to check if the answers generated by your RAG pipeline are supported by the retrieved context.
- You might want to implement checks for hallucinations, unsupported claims, or abstain when unsure.
- Look up how to compare generated answers to source documents, or how to use LLMs for evaluation.
- Try different evaluation strategies and see what works best!

Suggested steps:
1. Load the generated answers and their supporting context.
2. Implement a function to check if the answer is faithful to the context.
3. Print or save the evaluation results.

# Start your code below. Fill in the details as you experiment!
"""

# Example: Load answers and context (replace with your own logic)
"""
03_eval.py

This script is the fourth step in the RAG pipeline. It is responsible for evaluating the faithfulness of RAG answers or checking if the system should abstain from answering.

---
LEARNING GUIDE (do not delete):
- Your goal: Evaluate the quality of answers returned by your RAG system.
- Try to:
	* Load a set of questions, answers, and contexts (from previous steps or a test set).
	* Write a function to check if the answer is faithful to the retrieved context (e.g., does it hallucinate?).
	* Write a function to decide if the system should abstain (e.g., if not enough context is found or confidence is low).
	* Write a function to score or categorize answers (faithful, unfaithful, abstain).
	* Print or save your evaluation results (e.g., as a report or CSV).
- Look up 'faithfulness evaluation RAG', 'abstain in QA', or 'answer verification'.
- Add comments to explain your code and what you learned.

# Your code starts below

import json
import csv

def load_eval_data(filepath):
	"""Load evaluation data (questions, answers, contexts) from a JSON or CSV file. Returns a list of dicts with keys: question, answer, context."""
	# TODO: Implement this function
	pass

def is_faithful(answer, context):
	"""Return True if the answer is faithful to the context, False otherwise. You might use string matching, keyword overlap, or manual inspection."""
	# TODO: Implement this function
	pass

def should_abstain(context, min_length=30, min_score=None):
	"""Return True if the system should abstain based on context quality/amount. You might check if the context is too short, empty, or has low retrieval score."""
	# TODO: Implement this function
	pass

def evaluate_qa_pair(qa):
	"""Evaluate a single QA pair. Returns a dict with evaluation results."""
	result = {
		'question': qa.get('question'),
		'answer': qa.get('answer'),
		'context': qa.get('context'),
		'faithful': None,
		'abstain': None
	}
	result['faithful'] = is_faithful(result['answer'], result['context'])
	result['abstain'] = should_abstain(result['context'])
	return result

def save_eval_report(results, out_path):
	"""Save evaluation results to a JSON or CSV file."""
	# TODO: Implement this function
	pass

def print_eval_summary(results):
	"""Print a summary of evaluation results (counts, percentages, etc.)."""
	# TODO: Implement this function
	pass

def main():
	# Example usage (edit as needed):
	eval_data = load_eval_data('data/qa_pairs.json')
	results = [evaluate_qa_pair(qa) for qa in eval_data]
	print_eval_summary(results)
	# save_eval_report(results, 'data/eval_report.json')

if __name__ == "__main__":
	main()
