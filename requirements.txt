# Orchestration & env
langchain
langchain-core
langchain-community
langchain-text-splitters
chromadb==0.6.3
python-dotenv
pydantic
rich

# Document loaders / parsing
pypdf
unstructured
# unstructured[local-inference]  # optional: better PDF/HTML parsing; can omit if too heavy

# Embeddings / transformers
transformers
sentence-transformers

# Cloud LLMs (choose one or both)
langchain-openai
openai
anthropic
