# Orchestration & env
langchain
langchain-core
langchain-community
langchain-text-splitters
chromadb==0.6.3
python-dotenv
pydantic
rich

# Document loaders / parsing
pypdf
unstructured
# unstructured[local-inference]  # optional: better PDF/HTML parsing; can omit if too heavy

# Embeddings / transformers
transformers
sentence-transformers

# Cloud LLMs (choose one or more)
langchain-openai
openai

# Optional: Google Gemini support
# Uncomment to use Gemini models (gemini-pro, gemini-1.5-pro, gemini-1.5-flash, etc.)
langchain-google-genai

# Optional: Anthropic Claude support
# Uncomment to use Claude models (claude-3-opus, claude-3-5-sonnet, claude-3-haiku, etc.)
langchain-anthropic